This text was extracted from a document using optical character recognition (OCR) software and it contains OCR errors, especially when the original document was illegible. Can you use this information to fix the OCR errors in the text?  Return only the corrected text without adding any other comments.


Here are some further guidelines.
* Retain original abbreviations, such as “w/a” for “with a”; do not expand them.
* Preserve hyphens in line-broken words, even if they appear in odd places (e.g., "experi-ence").
* Some documents contain long alphanumeric codes; these are often not English words. Do not alter them.
* Some documents are multicolumn newspaper articles.  Sometimes the OCR software did not correctly infer the page layout or reading order.  It read the text line by line across multiple columns, mixing words from various columns instead of reading down the column..  
* Some documents are filled in forms which document a single UFO sighting.  In the original document, form fields were often enclosed in boxes with the field data appearing below the field title.  Sometimes the OCR software did not correctly infer the page layout or reading order. This causes the form field title and the form field data to be incorrectly paired. Infer the intended structure and reorder text accordingly.   For example “DATE Chicago, IL  LOCATION Jun 12, 1947” should be “DATE   Jun 12, 1947  LOCATION Chicago, IL” 
* Some form fields titles will be followed by a list of multiple options. Return all the corrected text including all the non-selected items in the original format.  In addition, if you believe an option was selected (e.g. marked with an X, a checkmark, or is circled) then surround the selected item(s) using the ʘ character.   For example if the original text is “Visible? Yes  xNo”, in that case you would return   “Visible? Yes  xʘNoʘ”
* Some documents contained headers, footers, handwritten margin notes and page numbers. Preserve all of this text.